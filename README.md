# Парсер текста на основе регулярных выражений

## Как использовать:
1. Создаёте объект-парсер, передаёте ему текст

```
parser = Parser(text)
```

2. Вызываете нужные методы, получаете в ответ списки

```
parser.extract_phones()
parser.extract_cards()
parser.extract_urls()
```
